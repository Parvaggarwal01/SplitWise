# Root convenience file for local development.
# The Go API reads these only if you export/source them before starting.
ADDR=:8080
DATABASE_URL=postgresql://USER:PASSWORD@HOST.neon.tech/DB?sslmode=require

